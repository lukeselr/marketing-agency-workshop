# Phase 5 — MCP Installation

Registers MCPs in `~/.claude.json` via `shared/secrets-handler.sh add-mcp`.

- `meta-ads-mcp` (community MCP server)
- `linkedin-ads-mcp` (community MCP server)
- Google Ads MCP — evaluate community options first; if none usable, ship thin wrapper at `~/mcp-servers/google-ads-mcp/`

Single Claude Code restart at end of Phase 5 covers all 3.
