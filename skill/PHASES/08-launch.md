# Phase 8 — Launch (PAUSED-by-default)

Per platform, creates campaigns + ad sets + ads via MCP. **Status = PAUSED.**

User reviews PAUSED campaigns in each platform's UI (or via MCP read), then says:
> "Approve and activate the Sydney workshop campaign on Meta + LinkedIn."

Skill flips status to ACTIVE only after explicit approval.

## Brand-protection guarantee

No campaign goes live without user approval — never auto-active. Audit trail in `.state/launches.log`.
