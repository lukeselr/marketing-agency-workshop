# Phase 7 — Creative Generation

`creative-generator` agent. Loads `voice-fingerprint.json` + 6 firewall files.

## Output

Per chosen platform: 30+ variants (6 hooks × 3 angles × 2 CTAs ≈ 36).
Each variant graded 5 axes. Hard-fail any firewall violation.

Variants written to `.state/creatives/<platform>/<concept>.md`.

## User review

User picks 3-5 winners per platform → into Phase 8.
